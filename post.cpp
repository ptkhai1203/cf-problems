#include <bits/stdc++.h>

using namespace std;
#define send {ios_base::sync_with_stdio(0);}
#define help {cin.tie(0);};

int main(){

    long long a, b;
    while(cin >> a >> b){
        cout << a + b;
    }

    return 0;
}